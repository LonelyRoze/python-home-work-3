<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jenkins Server Status</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th,
        td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }
    </style>
</head>

<body>
    <h2>Jenkins Server: {{ server }}</h2>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Status</th>
                <th>Timestamp</th>
                <th>Number</th>
            </tr>
        </thead>
        <tbody>
            {% for result in results %}
            <tr>
                <td>{{ result.get("name") }}</td>
                <td>{{ result.get("status") }}</td>
                <td>{{ result.get("result", {}).get("timestamp") or "-" }}</td>
                <td>{{ result.get("result", {}).get("number") or "-" }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>

</html>
